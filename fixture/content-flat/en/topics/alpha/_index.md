---
title: "Alpha (misplaced name)"
description: A directory named after the registered alpha section but sitting below a directory that is not a section at all.
weight: 1
---

This page is the discriminating case for positional section detection: the FIRST
registered key in its URL (`alpha`) is at the wrong position, so a match-anywhere
implementation picks it and a positional one does not.
