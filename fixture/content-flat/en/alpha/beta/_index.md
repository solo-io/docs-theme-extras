---
title: "Beta (name collision)"
description: A directory inside alpha that shares a name with the registered beta section, to pin that section detection is positional.
weight: 3
---

This directory is named after the registered `beta` section but sits BELOW the
alpha section, so it must be treated as ordinary content.
