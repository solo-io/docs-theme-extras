MARKER_LISTCONT_LEAD. A reused snippet whose body spans several lines, pulled
into a numbered step.

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: MARKER_LISTCONT_CODE
```

MARKER_LISTCONT_TAIL. Trailing prose after the fence, still part of the step.
