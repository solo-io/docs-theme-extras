> [!TIP]
> **MARKER_REUSE_TAB_LEAD.** A GitHub-style callout that lives in a reuse snippet and is pulled into a tab. This mirrors agentgateway's `kind-loadbalancer-tip.md`.
>
> - MARKER_REUSE_TAB_LIST1 first item
> - MARKER_REUSE_TAB_LIST2 second item
>
> ```sh
> # MARKER_REUSE_TAB_FENCE
> kubectl get svc agentgateway-proxy
> ```
